import { Message } from '../../Types';

const Chat = (chat: Message) => {
  return (
    <div className='chat'>
      <section>@atul</section>
      <section>Hey there!</section>
    </div>
  );
};

export default Message;
